using UnityEngine;
using System.Collections;

public class ScoreResult : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Debug.Log (scorescript.timer);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
